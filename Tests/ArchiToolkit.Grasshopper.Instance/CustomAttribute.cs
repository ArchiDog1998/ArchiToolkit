using Grasshopper.Kernel;
using Grasshopper.Kernel.Attributes;

namespace ArchiToolkit.Grasshopper.Instance;

public class CustomAttribute(IGH_Component component) : GH_ComponentAttributes(component);